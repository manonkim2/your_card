import Form from '@/components/signup/Form'

const SignupPage = () => {
  const handleSubmit = () => {}

  return (
    <div>
      <Form onSubmit={handleSubmit} />
    </div>
  )
}

export default SignupPage
